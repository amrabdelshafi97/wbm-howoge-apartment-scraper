version: '3.8'

services:
  apartment-scraper:
    build: .
    container_name: wbm-apartment-scraper

    # Resource limits to prevent EAGAIN errors
    mem_limit: 2g
    mem_reservation: 1g

    # Increase shared memory for Chromium (prevents spawn failures)
    shm_size: 2gb

    # File descriptor and process limits
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
      nproc:
        soft: 8192
        hard: 8192

    environment:
      - EMAIL_USER=${EMAIL_USER}
      - EMAIL_PASSWORD=${EMAIL_PASSWORD}
      - NOTIFY_EMAIL=${NOTIFY_EMAIL}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}

    volumes:
      - ./apartments-data.json:/app/apartments-data.json
      - ./logs:/app/logs

    restart: unless-stopped
    network_mode: bridge

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    # Uncomment for specific timezone
    # environment:
    #   - TZ=Europe/Berlin
